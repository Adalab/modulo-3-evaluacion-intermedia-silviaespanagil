import React from "react";

const PokemonDetail = (props) => {
  return <h3>{props.pokeDetail.name}</h3>;
};
/*const PokemonDetail = (props) => {
  return <h3>{props.pokemon.name}</h3>;
};*/
export default PokemonDetail;
